<%= error_messages_for :navigation_type %>
<% form_for [:admin, @navigation_type] do |form|%>
    <%= form.label :name %>
    <%= form.text_field :name %>   
  <br/>
          <p class="buttons">
          <%= save_model_button(@navigation_type) %>
          <%= save_model_and_continue_editing_button(@navigation_type) %>
          or
          <%= link_to "Cancel", {:action => :index} %>
        </p>
<%end%>