- if  (@page.parent != nil && @page.parent.parent == nil && @page.parent.class_name != "LanguageRedirectPage") || (@page.parent != nil && @page.parent.parent != nil && @page.parent.parent.parent == nil && @page.parent.class_name != "LanguageRedirectPage")
  .set
    %p
      %label{ :for => 'page_base_gallery_id'}
        Navigation Type
      = select :page, :navigationTypeId,  NavigationType.find(:all).collect{|g| [g.name, g.id]}.unshift(['None', nil])
